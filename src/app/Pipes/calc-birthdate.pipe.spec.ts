import { CalcBirthdatePipe } from './calc-birthdate.pipe';

describe('CalcBirthdatePipe', () => {
  it('create an instance', () => {
    const pipe = new CalcBirthdatePipe();
    expect(pipe).toBeTruthy();
  });
});
