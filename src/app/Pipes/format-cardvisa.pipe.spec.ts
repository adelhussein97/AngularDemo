import { FormatCardvisaPipe } from './format-cardvisa.pipe';

describe('FormatCardvisaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCardvisaPipe();
    expect(pipe).toBeTruthy();
  });
});
